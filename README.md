# InventorySystem

![Zenject](https://img.shields.io/badge/Zenject-purple?logo=unity&logoColor=white)
![Лицензия](https://img.shields.io/badge/Лицензия-MIT-green)

Система инвентаря для Unity, вдохновлённая игрой **Genshin Impact**. Главная идея состоит в разделении предметов на разные инвентари по типу предмета (еда, оружие, расходники).

---

## 🌟 Особенности системы

- **Разделение предметов по типам** — каждый тип предметов хранится в отдельном инвентаре
- **Полная расширяемость** — создание новых типов предметов без изменения ядра системы
- **Типобезопасная архитектура** — использование обобщённых (`generic`) классов и интерфейсов
- **Интеграция с Zenject** — чистая настройка зависимостей через контейнер
- **Гибкая визуализация** — отдельные визуализаторы для каждого типа инвентаря
- **Неограниченное количество типов предметов** — добавляйте столько категорий, сколько требуется

---

## 🔧 Как создать свой тип предмета

### Шаг 1: Создайте интерфейс

Создайте интерфейс, реализующий `IInventoryItem`:

```csharp
public interface IFoodItem : IInventoryItem 
{
    // Дополнительные свойства, специфичные для типа предмета
}
```

### Шаг 2: Создайте ScriptableObject

Создайте `ScriptableObject`, реализующий ваш интерфейс :

```csharp
[CreateAssetMenu(menuName = "Inventory/FoodItem", fileName = "NewFoodItem")]
public class FoodItemConfig : ScriptableObject, IFoodItem
{
    // Реализация интерфейса и данные предмета
}
```

---

## ⚙️ Настройка через Zenject

### Регистрация инвентарей

Чтобы создать инвентарь под новый тип предмета, забиндите его в Installer (Zenject):

```csharp
Container.BindInterfacesAndSelfTo<Inventory<IFoodItem>>().AsSingle().NonLazy();
Container.BindInterfacesAndSelfTo<Inventory<IWeaponItem>>().AsSingle().NonLazy();
Container.BindInterfacesAndSelfTo<Inventory<IUsableItem>>().AsSingle().NonLazy();
```

### Регистрация визуализаторов

Для отображения инвентаря следует забиндить `InventoryVisualizer` с соответствующим типом предмета. Для него нужен бинд `InventoryWindow`, который является `MonoBehaviour` объектом на сцене:

```csharp
Container.BindInterfacesAndSelfTo<InventoryVisualizer<IFoodItem>>().AsSingle().NonLazy();
Container.BindInterfacesAndSelfTo<InventoryVisualizer<IWeaponItem>>().AsSingle().NonLazy();
Container.BindInterfacesAndSelfTo<InventoryVisualizer<IUsableItem>>().AsSingle().NonLazy();
```

---

## 🎮 Демонстрационная сцена

В демо сцене приведена реализация использования системы инвентаря. В ней можно:
- Переключать вкладки инвентаря по типу предмета
- Отображать информацию о выбранном предмете
- Взаимодействовать с предметами через UI
  
<img width="1579" height="884" alt="image" src="https://github.com/user-attachments/assets/92c0a6fa-3aa3-42b2-b81d-9eadb7da7001" />

---

## ➕ Расширяемость

Количество новых типов предметов не ограничено. Главное — привязать их инвентари и визуализаторы, чтобы к ним можно было обращаться через систему. Для добавления нового типа достаточно:

1. Создать интерфейс, наследующий `IInventoryItem`
2. Создать `ScriptableObject`, реализующий этот интерфейс
3. Забиндить инвентарь и визуализатор в Zenject Installer

